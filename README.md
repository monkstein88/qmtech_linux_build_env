# qmtech_c5soc_kfb_dual_sdram_linux_build_env
This is just an environemnt containint the scripts, recipes, directories, etc. to build the entire Linux image (uboot + Linux binaries) for the QMTECH board
